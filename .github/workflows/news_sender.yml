name: Send News

on:
  schedule:
    - cron: '*/120 * * * *'    # Chạy mỗi 30 phút
  workflow_dispatch:

jobs:
  send:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4.2.0
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Fetch latest sent_articles.json from remote
        run: |
          git fetch origin ${{ github.ref_name }}
          git checkout origin/${{ github.ref_name }} -- sent_articles.json

      - name: Set up Python
        uses: actions/setup-python@v5.3.0
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run bot
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
        run: python bot.py

      - name: Commit and push sent_articles.json if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add sent_articles.json
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Update sent articles [skip ci]"
            git push
          fi
